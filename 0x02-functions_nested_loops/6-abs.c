#include "main.h"
/**
 * _abs - checks the absolute value of a number
 * @j: integer to be checked
 * if - conditional statement for the varibales to be checked
 *
 *
 *Return: j if poistive, otherwise -j
 */

int _abs(int j)
{

if (j >= 0)
{
	return (j);
}
else
{
return (-j);
}
}
