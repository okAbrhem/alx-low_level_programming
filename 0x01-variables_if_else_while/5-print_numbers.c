#include<stdio.h>

/**
 * main - Entry point for executing the code block
 * a - varibale to store single digit numbers
 * printf - used to print out strinn and integers to stdout
 *
 *
 *Return: Always 0(Success)
 */

int main(void)
{
	int a;

	while (a < 10)
	{
	printf("%d", a);
	a++;
	}
	printf("\n");
return (0);
}

