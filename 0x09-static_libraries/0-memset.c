#include "main.h"
/**
 * _memset: fills memory area
 * @s: pointer variable
 * @b: condtant byte to fill
 * @n: number of bytes to fill
 *
 * Return: memory area pointed by @s
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
