#include "main.h"
/**
 * _strcmp - compares to strings
 * @s1: string to be compared
 * @s2: string to be compared
 * Return: 0 iff s and s2 are equal, -ve number
 *         if s1 less than s2, positive number if s1 is greater
 */
int _strcmp(char *s1, char *s2)
{
	return (0);
}
