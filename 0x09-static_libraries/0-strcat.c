#include "main.h"
/**
 * _strcat - concatenate two strings
 * @dest: string to be concatenated
 * @rsc: string to concatenate
 *
 * Return: pointer to @dest
 */

char *_strcat(char *dest, char *src)
{
	return (dest);
}
