#include "main.h"
/**
 * _strpbrk - search a string for any of a set of bytes
 * @s: string to be checked
 * @accept: matching charatcer
 *
 * Return: matching pointer to @s
 */
char *_strpbrk(char *s, char *accept)
{
	return (accept);
}
